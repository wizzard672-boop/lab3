# Отчёт по лабораторной работе: Рекурсия и итерация

## 1. Условия задач

### Задача 1. Перестановки длиной k
Функция для вычисления всех перестановок списка длиной `k`.
**Пример:**
k_permutations([1, 2, 3], 2)
[[1, 2], [1, 3], [2, 1], [2, 3], [3, 1], [3, 2]]

Задача 1: Перестановки длиной k
Рекурсивная реализация (k_permutations_recursive):

База: Если k == 0, возвращается пустой список внутри списка [[]] (пустая перестановка). Если k больше длины массива, возвращается пустой список (перестановок нет).

Шаг: Для каждого элемента массива current берётся этот элемент, а из оставшихся элементов (remaining) рекурсивно ищутся перестановки длиной k-1. Затем current добавляется в начало каждой найденной подстановки.

Итеративная реализация (k_permutations_iterative):

Использует встроенную библиотеку itertools, а именно функцию itertools.permutations(arr, k), которая является генератором, написанным на C и работающим по итеративному алгоритму (эквивалентно вложенным циклам).

Результат преобразуется в список списков для соответствия условию.


Задача 2: Рекуррентная последовательность

<img width="442" height="42" alt="image" src="https://github.com/user-attachments/assets/fd927aa8-60a9-4281-ad9f-ed48742acb0f" />

Рекурсивная реализация (sequence_recursive):

Внутренняя функция calc(i) реализует прямую рекурсию: при i == 0 возвращает 0, иначе вычисляет i * calc(i-1) + 1.0/i.

Примечание: Для вывода всей последовательности до n рекурсия вызывается n раз в цикле. Для больших n это неэффективно из-за повторных вычислений, но наглядно демонстрирует рекурсивный подход.

Итеративная реализация (sequence_iterative):

Создаётся список result с начальным значением [0.0] (x_0).

В цикле от 1 до n каждый следующий член вычисляется на основе предыдущего сохранённого значения: next_val = i * result[-1] + (1.0 / i). Это наиболее эффективный способ для данной задачи.

<img width="699" height="222" alt="image" src="https://github.com/user-attachments/assets/45f0dc13-9ebe-49c9-8b1b-6d35d45a022d" />

Ссылки на используемые материалы

Документация Python по модулю itertools: docs.python.org/3/library/itertools.html#itertools.permutations

Понятие рекурсивного обхода дерева для генерации перестановок: Алгоритм "Backtracking" (поиск с возвратом).

Методические указания к лабораторной работе.
